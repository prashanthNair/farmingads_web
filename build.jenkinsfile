

pipeline{ 
  agent any
stages {
  stage('Build') {
    steps {
      sh 'npm run build'
    }
  }

  stage('Deploy') {
    steps {
       sh 'npm run build'
    }
  }

  stage('Destroy') {
    steps {
     sh 'npm run build'
    }
  }
}
}