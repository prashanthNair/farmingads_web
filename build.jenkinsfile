pipeline{ 
stages {
  stage('Build') {
    steps {
      sh 'npm run build'
    }
  }

  stage('Deploy') {
    steps {
      sh 'cdk deploy --require-approval=never'
    }
  }

  stage('Destroy') {
    steps {
     sh 'npm run build'
    }
  }
}
}